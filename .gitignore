node_modules
.DS_Store
.env
.env.local
server/.env
dashboard/.env.local
server/dist
dashboard/.next
server/node_modules
dashboard/node_modules

# TLS material for nginx (keep paths private)
nginx/ssl/*.pem
nginx/ssl/*.key

# Let's Encrypt data (private keys — never commit)
certbot/conf/
certbot/www/.well-known/
